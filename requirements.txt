# Runtime requirements (keep minimal for Cloud deployment).
pdf2image
pytesseract
pillow
opencv-python-headless
google-generativeai
streamlit
# Pin protobuf to a version compatible with generated protos used by Streamlit and some deps
# Pin protobuf to a version compatible with generated protos used by Streamlit and some deps
protobuf==3.20.3
# Local imghdr shim package to ensure the imghdr module is available during
# dependency checks in hosted runtimes.
-e local_packages/imghdr_pkg